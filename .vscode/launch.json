{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Backend: Server (QA mode)",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}/backend/scripts/runserver.sh",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Backend: Server (--debug)",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}/backend/scripts/runserver.sh --debug",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Backend: Server (--test)",
      "type": "node-terminal",
      "request": "launch",
      "command": "${workspaceFolder}/backend/scripts/runserver.sh --test",
      "cwd": "${workspaceFolder}"
    },
    {
      "name": "Frontend: Server (Vite)",
      "type": "node-terminal",
      "request": "launch",
      "command": "npm run dev",
      "cwd": "${workspaceFolder}/frontend"
    },
    {
      "name": "Python: Attach to Django (debugpy)",
      "type": "debugpy",
      "request": "attach",
      "connect": {
        "host": "localhost",
        "port": 5678
      },
      "justMyCode": true,
      "pathMappings": [
        {
          "localRoot": "${workspaceFolder}",
          "remoteRoot": "."
        }
      ]
    },
    {
      "name": "Frontend: Debug (Vite + Firefox)",
      "type": "firefox",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/frontend",
      "reAttach": true,
      "preLaunchTask": "npm: dev (frontend)",
      "serverReadyAction": {
        "pattern": "Local:\\s+http://localhost:([0-9]+)",
        "uriFormat": "http://localhost:%s",
        "action": "openIntegratedBrowser"
      }
    }
  ],
  "compounds": [
    {
      "name": "Fullstack: Debug (Backend + Frontend + Firefox)",
      "configurations": [
        "Backend: Server (QA mode)",
        "Frontend: Debug (Vite + Firefox)"
      ]
    }
  ]
}